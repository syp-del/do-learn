# Do-Learn Do-Learn (두런두런) — Claude 작업 안내

초등학교 1학년 쌍둥이 자매를 위한 읽기·낱말·할 일·영어 스피치 앱.
단일 `index.html` + Vercel 서버 함수(`api/`) + Supabase. `main`에 푸시하면 Vercel이 자동으로 배포한다.

## 가장 중요한 원칙 — 아이들이 즐거운 마음으로, 재미있게!

아이가 보는 모든 화면·문구·효과는 이 원칙을 따른다. 새 기능을 만들 때도, 고칠 때도 이 관점으로 다시 본다.

- "틀려도 괜찮아", "할 수 있어!", "너무 잘하고 있어!" 같은 응원이 곳곳에 있다.
- "틀렸어/실패/오답" 대신 이렇게 말한다.
  - "괜찮아요! 정답은 ○○예요."
  - "이렇게 말해보면 더 좋아요."
  - "한 번만 더 해볼까?"
- 결과보다 노력을 센다: 말해 본 횟수, 끝까지 한 것, 스스로 확인한 것. 별은 최소 1개다.
- 비교는 가볍게 한다.
  - 쌍둥이끼리 겨루기보다 '어제의 나'와 비교하고, 함께 모으기를 둔다.
  - 왕관은 여러 분야에 두어 둘 다 받을 수 있게 한다.
- 스스로 고르고(자율) → 해보고 → 돌아보는 흐름으로 만든다. 부모님은 메뉴를 준비하고, 고르는 건 아이다.
- 한 번에 5~10분, 하나씩 한다. 단추는 크게, 소리와 그림 힌트를 함께 둔다.
- 오류 문구도 아이 말투로 쓴다(예: "뚜뚜가 잠깐 쉬고 있어요"). 부모님용 안내는 따로, 정확하게 쓴다.
- AI는 답을 주기보다 질문한다. 아이의 이름·목소리 같은 개인 정보는 필요한 만큼만 보낸다.

## 아이별 맞춤 (성향)

부모님 화면 → 아이 → 성향에서 정한다(`profile.style`, 함수 `kidStyle(k)`). **코드에 아이 이름을 넣지 않는다.**

| 칸 | 값 | 앱이 하는 일 |
|---|---|---|
| `compete` | `high` 도전을 좋아해요 | 기록 깨기, "오늘의 도전!" |
| | `low` 차분히 하는 게 좋아요 | 어제의 나와 비교, 압박 없는 말 |
| `pace` | `quick` 척척 빨라요 | "천천히, 또박또박", 퀴즈에서 고른 뒤 "✔ 이걸로 할래요"로 한 번 더 확인, 확인 습관 칭찬 |
| | `careful` 꼼꼼해요 | 한 단계 깊은 도전(외우기 단계 건너뛰기, 퀴즈 보기 하나 더), 표현·자신감 칭찬 |
| `strengths` | `leader` | 가르쳐 주기 미션 |
| | `smart` | 더 알아보기 |
| | `bright`, `focus` | (아직 쓰는 곳 없음) |

실제 두 아이의 성향 메모는 `CLAUDE.local.md`에 있다. 이 파일은 저장소에 올리지 않는다(저장소가 공개).

## 아이들의 영어 수준 (난이도 기준)

- 두 아이 모두 영어유치원 출신이다.
- AR(STAR Reading) 지수가 3점대까지 나왔다가, 초등학교에 입학한 뒤 **2.4~2.6**으로 내려왔다.
- 영어 원서는 YBM ECC 학원에서 읽는다.
- 뚜뚜 원고 연습, 퀴즈, 추천 낱말은 **AR 2~3 수준(미국 초등 2학년 초반)**에 맞춘다.
  - 너무 쉬운 것보다 살짝 도전이 되는 쪽(ZPD)을 고른다.
  - 발음과 억양은 원어민 아이처럼 자연스럽게를 목표로 하되, 칭찬을 먼저 한다.
- 책장에 AR을 보여 준다.
  - 부모님 화면 → 아이에서 아이의 AR 점수(`profile.arScore`, 넣은 날 `arAt`)를 넣는다.
  - 책을 넣거나 고칠 때 그 책의 AR(`book.ar`)을 적는다. 책 뒤표지나 AR BookFinder에 나온다.
  - 책 AR − 아이 점수로 나눈다: −0.5보다 낮으면 😊 술술 책, +0.7까지는 👍 딱 맞는 책, 그 위는 📈 도전 책. 셋 다 칭찬한다(자신감·실력·용기).
- 아이별 자세한 메모는 `CLAUDE.local.md`에 있다.

## 구조

- **`index.html`**: 앱 전체.
  - 탭: 오늘 · 책장 · 단어장 · 받아쓰기 · 스피치 · 생각 · 세계 · 배움기록 · 놀이 (폰처럼 좁으면 탭 줄을 옆으로 민다)
  - 새 기능 단추는 `data-act="이름"` + `data-arg`로 달고, 함수는 `ACTS.이름`에 둔다(클릭 처리 맨 앞에서 부른다).
  - 창(모달) 안 입력칸은 `data-keep="키"`를 달면 칠 때마다 `kv(키)`에 담긴다 → 창을 다시 그려도 글자가 남는다(`kvField`).
  - 동기화: 내가 쓰는 중(`SYNC`)에 받아 온 옛 기록은 버린다. 방금 쓴 답이 몇 초 동안 사라져 보이던 문제를 막는다.
  - 📒 배움기록: `weekStatsAt`(이번 주·지난주), `crownPlan`(반짝 왕관 나누기), 💌 칭찬 스티커(`kudos` 컬렉션)
  - 👪 부모님 계정 `parent*`(부모님 화면 → 👪 부모님): 대표 관리자(owner)는 처음 정한 부모님 비밀번호(`settings.pin`)의 주인이고 닉네임은 `settings.ownerNick`(비우면 "뚜뚜아빠"). 다른 부모님은 `settings.parents` [{id, nick, avatar, pinHash}] — 비밀번호는 섞은 값만(`parentPinHash`), 서로 겹치지 않게. 비밀번호를 누르면 누구인지 알아본다(`parentByPin` → `S.parentId`). 부모님 추가·삭제는 대표 관리자만.
    - 👪 누구세요?(`parentWhoSheet`): 처음 정한(대표) 비밀번호로 부모님 화면·대화방에 들어오면 닉네임을 고르거나(`paWho`) ➕ 새 닉네임을 만든다(따로 비밀번호 두 번, `paWhoSave` — 겹치는 숫자·1234는 안 된다). 자기 비밀번호로 들어오면 이 창 없이 바로. 승인·지구본 확인 같은 다른 용도도 이 창 없이.
    - 들어온 상태(`parentOn`/`parentTouch`/`parentOff`, `S.parentId`+`S.parentAt`): 비밀번호로 누구인지 알아본 뒤 아무것도 안 하고 15분(`PARENT_TTL`)이 지날 때까지 기억한다. 그동안 대화방은 비밀번호 없이 그 닉네임으로 열린다(아이 공간이 열려 있으면 아이로 열리고, 부모님 칩을 누르면 바로 바뀐다). 아이가 그림 비밀번호로 들어가면(`kidEnterNow`) · 🔒 나가기(부모님 화면 `parentOut`, 대화방 `chatParentOut`)면 끝. 부모님 화면·놀이 승인·지구본 확인은 늘 비밀번호를 묻는다(아이가 스스로 허락하지 못하게).
    - 텔레그램 연결·알림은 대표 관리자만. 텔레그램 "열어주러 가기" 단추 주소에는 `&tg=1`이 붙고(`api/notify.js`), 그렇게 연 승인은 대표 관리자 비밀번호로만 된다(`S.fromTelegram`). 앱 안의 🔔 승인 카드·놀이 승인 탭은 모든 부모님이 할 수 있다.
    - 대화방·칭찬 스티커에서 부모님은 `from 'p:아이디'`(닉네임으로 보인다). 예전 `'parent'` 글은 "엄마 아빠"로, 방 멤버의 `'parent'`는 부모님 모두.
  - 👪 부모님 모드(`S.parentView`, `parentStart`/`parentEnter`/`parentLeave`): 부모님 관리 → 비밀번호 → (대표 비밀번호면 누구세요?) → 담당 아이(처음 한 번 고르면 기억한다: `settings.ownerKid` / `parents[].kid`, `parentKidOf`) → **그 아이와 똑같은 화면(아래 탭) + 맨 끝 👪 부모님 관리 탭**(`viewParentTab` — 예전 부모님 창의 내용이 모두 여기에). 머리줄 이름(부모님 그림 + 👀 아이)을 누르면 아이를 바꾸고(`parentSwitch`), 🏠·🔒 나가기면 끝. 들어온 상태가 끝나면(15분) 시작 화면으로 돌아간다(`draw` 맨 앞 · 1초 시계).
    - 부모님 관리 화면은 어디서든 `parentShow()`로 다시 그린다 — 부모님 모드면 탭을 바로 그리고(`draw`), 아니면 창(`openModal(parentSheet())`). 탭이 보이는지는 `parentVisible()`. 아이가 아직 없으면 예전처럼 창.
    - 부모님 모드에서는 아이의 알림 카드를 띄우지 않고(`notiCheck`), 같이 놀기 접속 표시도 하지 않는다(아이가 들어온 게 아니다). 대화방은 부모님으로 열린다. 부모님 관리 탭에서 이름 없는 칸에 쓰는 중이면 5초 동기화로 다시 그리지 않는다(`render`).
    - 겹치는 것은 뺐다: 📂 기록 관리(칩 맨 끝)에는 아이 화면에서 못 고치는 것만(한 일 기록·생각·도장·칭찬·대화·약속·받아쓰기·철자) — 책·단어·여행·가고 싶은 곳은 아이 화면에서, 할 일은 '할 일' 칸에서(✏️는 숨은 `todos` 칸으로 고치고 끝나면 '할 일'로 돌아온다, `pdDone`). 설정의 비밀번호 바꾸기는 👪 부모님 칸의 '내 비밀번호 바꾸기'로 하나만. 부모님 모드에서는 탭 맨 위 제목·나가기 대신 머리줄 🔒(🏠 자리).
    - 부모님 모드에서는 보고 있는 아이의 놀이 요청도 화면 위 카드로 보이고, 승인은 비밀번호 없이(부모님 관리 탭의 열어주기와 같다 — 텔레그램으로 연 승인만 대표 비밀번호). 🤝 같이 놀기는 부모님으로 들어간다(`vsOpen`).
  - 🧹 새로 시작하기(부모님 관리 → 설정 맨 아래, `resetCard`/`ACTS.resetGo`, 대표 관리자만, '지우기'를 적어야 된다): 아이별 기록(`RESET_COLLS` + 음성 `vclip` + 샘플 원고·급수표)을 모두 지우고 프로필의 별·놀이 기록·알림 기록을 비운다. 남는 것: 샘플이 아닌 스피치 원고·급수표, 아이 이름·그림·색·성향·영어 이름·AR·그림 비밀번호, 부모님 계정, 설정, 사전 저장(`dict`).
    - 부모님 모드에서 고치고 넣고 지우기: 오늘 화면의 모든 할 일에 ✏️(`pTodoEdit` — 제목·그림·요일)·🗑(`pTodoDel`), '+ 할 일'은 부모님이 정한 할 일(`createdBy 'parent'`). 단어장은 ➕ 직접 넣기(`wordAdd`)·✏️ 뜻 고치기(`wordEdit`)·🗑. 책은 책 화면의 고치기·지우기와 ＋.
    - 🎈 놀이는 허락 없이 둘러본다(맨 위 카드에서 그 아이의 놀이 시간을 열어 주기·잠그기). 부모님이 한 놀이는 아이의 별·기록에 넣지 않는다(`awardStars`·`playLevelUp`·`itSave`·`bhSave`·`petSave`가 부모님 모드면 멈춘다). 1초 시계도 부모님 모드의 놀이는 닫지 않는다.
  - ➕ 사용자 추가(시작 화면, `#addUser` → `userAddSheet`): 🧒 아이는 부모님 비밀번호 → 부모님 모드의 아이 칸(`S.view.pmQuick`로 아이 고르기 없이), 👪 부모님은 대표 관리자 비밀번호(`pinPurpose 'addParent'`) → 새 닉네임·비밀번호.
  - 💬 대화방 `chat*`(오른쪽 위 💬 → 오른쪽에서 열림, `#chatWrap`): 👪 우리 가족방(모두, `id 'family'`)은 늘 있고, ➕ 새 방은 초대할 사람을 골라 만든다(컬렉션 `chatrooms` {name, emoji, members[아이 id | 'parent'], createdBy}). 글은 `chat` {room, from, text, at}, 칭찬 스티커는 `kudos` {…, room}(배움기록·왕관이 센다). room 이 없으면 가족방. 방에 없는 사람은 누구든(엄마 아빠도) 그 방에 들어가거나 글을 볼 수 없다(`chatRooms`·`chatCanSee`·`chatGo`, 부모님 기록 관리의 대화·칭찬도 `pdRoomOk`로 같게). 엄마 아빠로 쓰려면 부모님 비밀번호(`pinPurpose 'chat'`, 닫으면 끝). 안 읽은 수는 방마다(아이 `profile.noti.chatSeen`, 부모님 localStorage), 💬 숫자는 합. 5초 동기화 때는 보고 있는 목록만 다시 그린다(`chatRefresh`). 예전 쪽지(`settings.parentNote`)는 가족방 📌 — 부모님만 옆의 '떼기'(`chatNoteOff`). 부모님 화면 → 설정의 쪽지 안내는 없앴다.
    - 🎤 음성 메시지 `vc*`/`chatVoice*`: 30초까지 32kbps로 녹음해 가족 데이터의 따로 칸 `vclip` {mime, secs, b64, at}에 둔다. `sbAllItems`가 `vclip`은 빼고 읽어서(5초 동기화가 무거워지지 않게) 누를 때만 받아 오고, 받은 건 이 기기 IndexedDB(`tts` 칸, 키 `vclip|id`)에 둔다. 2주가 지나면 지운다(`vcCleanup`). 글은 `chat` {kind:'voice', clip, secs}. AI로는 보내지 않는다. 방·글을 지우면 소리도 지운다(`vcDelete`).
  - ✨ 시작 화면 메모지 `aff*`: 긍정 한마디 16장(`AFFIRMS`, 아이가 스스로 하는 말 "나는 할 수 있어!")이 6초마다 넘어간다. 날마다 다른 장부터, 누르면 다음·옆으로 밀면 앞뒤·🔊 읽어 주기. 5초 동기화로 다시 그려도 보던 장(`AFF.i`)에서 이어지고, 넘길 때는 글자만 바꾼다. 예전의 부모님 쪽지 메모지는 없앴다(부모님 요청 — 쪽지는 대화방·오른쪽 카드로).
  - 🔐 그림 비밀번호 `kp*`/`kidEnter`: 시작 화면에서 아이를 고르면 그림 9개 중 4개를 순서대로(`KPIN_PICS`). 처음이면 아이가 정하고(두 번), `profile.kpin`에는 섞은 값(`kpinHash`). 잊으면 부모님 비밀번호(`kidin:아이id`). 부모님 화면 → 아이에서 지운다. 다른 아이 공간에 들어가지 못하게 — 배움기록·대화방은 함께 본다.
  - 💮 참 잘했어요 도장 `stampSvg()`: 할 일을 끝내면 줄 오른쪽과 이번 주 칸에 찍힌다(예전 🍓 대신).
  - 💌 쪽지·칭찬 알림 `noti*`: 부모님 쪽지(`settings.parentNote`)와 받은 칭찬 스티커가 받는 아이 화면 오른쪽에 카드로 뜬다(`#notiBox`, 창·놀이 층 아래 z 58). "고마워요"를 누르면 `profile.noti`(`since`·`seen`·`noteAt`)에 기억한다. 처음 쓰는 날엔 지난 하루 것만. 창·층이 열려 있으면 닫힌 뒤에 띄운다. 폰은 1장, 아이패드는 2장씩.
  - 📚 책장: `book.status`가 `'toread'`면 📘 읽을 책(`addedAt`, `readAt` 없음), 아니면 📗 읽은 책(`bookRead(b)`, 예전 책은 status가 없다). 등록할 때 "다 읽었어요 / 읽을 예정이에요"를 고른다. 읽은 책만 별점 → 어려웠던 낱말 → 한 줄 느낌(`book.feel`)을 묻는다. 읽을 책장에 있던 책의 바코드를 다시 찍거나 "📗 다 읽었어요!"를 누르면 읽은 책이 된다. 한 주의 책 수는 읽은 책만 센다.
  - 📂 부모님 화면 → 기록 관리(`pdTab`, `PD_KINDS`): 위쪽 아이 탭(`pd-kidtab`)으로 아이를 고르고, 아이마다 할 일·한 일 기록·책장·단어·생각·여행·가고 싶은 곳·도장·칭찬·약속·받아쓰기·철자를 줄 안에서 ✏️ 고치고 🗑 지운다(비밀번호 화면을 떠나지 않게 줄 안에서 한 번 더 묻는다). 책 고치기는 `bookEditOpen(id, 'parent')`. 함께 간 여행은 그 아이만 뺀다. 별 개수는 아이 탭에서 고친다.
  - 부모님 화면 → 할 일도 위쪽 아이 탭(`pd-kidtab` 모양, `S.view.todoKid`, `ACTS.todoKid`)으로 아이마다 따로 본다(탭에 할 일 수).
  - 🫧 단어 가방: 아이가 🗑로 직접 뺄 수 있다(`ACTS.wordDel`, 한 번 더 묻는다).
  - 주요 구역: `Store`(저장), `sbPull`(5초 동기화, 1000줄씩 나눠 읽기), `view*()`(화면), 이벤트 배선
  - 🦜 뚜뚜 스피치 교실: `tt*` 함수와 `TT` 상태(`kind`: `lesson` 수업 · `ecc` ECC speech 연습 · `stage` 처음부터 끝까지)
    - 🎤 ECC speech 연습 `ecc*`: 원고를 한 문장씩 읽는다(그림 힌트·시간 제한 없음). 🔊 뚜뚜 목소리로 듣기 → 🎤 읽기 → 기기 음성 인식(`webkitSpeechRecognition`, en-US)이 알아들은 단어가 바로 초록색(`eccLive`). 다 읽으면 🟢 잘 읽은 단어 · 🔴 더 연습할 단어(`eccAlign`: 숫자=글자, E C C=ECC, 이름 칸은 늘 맞음). 빨간 단어를 누르면 "여기 이렇게 말해 보자!" + 천천히 들려주기 + 팁(`eccTip`). AI 듣기가 켜져 있으면 발음·유창성·억양은 Gemini가 본다. 문장마다 마지막 색·가장 좋은 별은 `prog.ecc[i]`. 녹음과 인식이 부딪히면(아이패드 — 인식은 됐는데 녹음이 조용함) `ECC.srOff`로 녹음만 쓴다. 부모님 화면 → 스피치에서 초록 표시를 끌 수 있다(`settings.eccSr 'off'`).
    - 🔁 다시 해보기는 언제나 있다(수업 피드백·녹음 중 "처음부터 다시"·ECC 연습).
    - 원고의 `{name}`은 아이 영어 이름(`profile.nameEn`, 부모님 화면 → 아이)으로 바뀐다(`ttNameFill`). 이름은 가리는 낱말(`ttMaskWords`)에 들어가서, 이름이 든 문장은 AI 듣기로 보내지 않고 영어 읽기도 기기 목소리로만 한다.
  - 🧩 단어 퀴즈: `quiz*`, `koVariants`(헷갈리는 맞춤법 보기)
  - 목소리: `voice()` / `nvPlay()` (Gemini 자연스러운 목소리) / `say()` (기기 목소리)
    - 읽기 전에 `speechText()`로 이모지·물결(~)·따옴표·괄호를 뺀다(기기 목소리가 "파티 폭죽", "물결"이라고 읽었다). 이모지에 바로 붙은 조사도 함께 뺀다("🔊로 먼저" → "먼저", 안 그러면 "로 먼저"라고 읽는다). `say()`는 글자 수를 그대로 둬서 단어 불켜기가 어긋나지 않는다.
    - 한국어 속 숫자는 `koNumSpeak()`로 우리말로 바꿔 읽는다("3번"을 "쓰리번"으로 읽었다): 세는 말 앞은 한·두·세(`koNative`), 나머지는 일·이·삼(`koSino`), 달은 유월·시월. 뚜뚜가 말하는 몇 번째는 `koOrd(n)`("두 번째 문장이야").
    - 🔇 아이패드: 녹음하면 기기가 '녹음+재생' 모드가 되어 녹음 뒤에 뚜뚜 목소리·내 목소리가 안 났다. `recRelease`가 `audioSessionPlay()`로 `navigator.audioSession.type = 'playback'`, 소리 크기를 재던 AudioContext를 닫고, 뚜뚜 목소리 칸은 다음에 누를 때 새로 만든다(`NV.needFresh` → `nvUnlock`). 소리를 내는 단추에서는 늘 `nvUnlock()`을 먼저 부른다.
    - 자연스러운 목소리는 문장을 열 때 미리 받아 둔다(`nvPrefetch`, 하나씩 — 한국어는 `koNumSpeak`을 거친 글로). 6초(`nvPlay`의 `wait`) 넘게 걸리면 이번엔 기기 목소리로 읽고, 받아 온 건 다음에 쓴다. 서버는 한 문장에 5초쯤 걸린다 — 그래서 처음 쓰는 기기(갤럭시 탭)는 AI 목소리를 골라도 기기 목소리가 나기 쉬웠다. 이 기기가 AI 목소리면 받아쓰기를 열 때 문장을 미리 받아 둔다(`dtOpen`).
    - ⚠️ **Gemini 무료 등급은 목소리 모델마다 하루 10번쯤만 새로 만든다**(서버 기록: "limit: 10 requests per day on Free Tier"). 그래서:
      - 서버(`api/coach.js` `tts`)는 한도(429)에 걸린 모델을 10분 쉬게 하고(`ttsTired`) 다음 모델(`TTS_MODELS`)로 만든다. 모두 한도면 busy.
      - 🗂 한 번 만든 목소리는 가족 데이터의 따로 칸 `ttsclip` {k, b64, at}에 올려 모든 기기가 같이 쓴다(`nvShareGet`/`nvShareUp`, id = SHA-256(가족 ID + 키)로 만든 uuid). 5초 동기화에서는 뺀다(`sbAllItems`). 이 기기에만 있던 목소리도 틀 때 없으면 올린다. 120일 지난 건 지운다.
      - 같은 말을 미리 받기·누르기가 겹쳐도 한 번만 만든다(`NV.inflight`). 서버가 쉬는 중(`nvRest`, 한도·오류)에도 만들어 둔 목소리(이 기기·가족 저장)는 AI 목소리로 튼다 — 새로 만들지만 않는다.
    - `<audio>`로 못 틀면(브라우저가 막음·파일을 못 읽음) Web Audio로 한 번 더 튼다(`nvPlayWA`), 그래도 안 되면 기기 목소리. 어떤 목소리로 났는지는 `NV.last` {how:'ai'|'device', why, ms}, 부모님께는 `nvWhyText`로 알려 준다. 부모님 화면 → 스피치의 🔊 들어보기(AI면 20초까지 기다린다)와 🔧 이 기기 소리 점검(`ACTS.sndCheck`, `sndEnv`: 브라우저·기기 목소리·AI 목소리 결과를 줄로 보여 준다).
    - 기기 목소리 고르기(`pickVoice`/`voiceScore`): 안드로이드(갤럭시)는 목소리 목록이 늦게 와서 비어 있으면 잠깐 기다린다(`voicesReady`). 기기에 깔린 목소리(local)를 인터넷 목소리보다 먼저, 고른 목소리의 말 표시(`ko_KR`)를 `u.lang`에 맞춘다. 목록이 늦게 오면(`voiceschanged`) 설정 화면의 빈 목록을 다시 그린다.
    - 🇰🇷 한국어 목소리는 **기기마다** 고른다(부모님 화면 → 스피치): 📱 기기 목소리 / 🎙 AI 목소리(`koMode()`, localStorage `dolearn.koMode` — 없으면 예전 가족 설정 `settings.koVoice`), 목소리(`dolearn.koVoice`), 빠르기·높이(`dolearn.koRate`·`koPitch`, `KO_RATES`·`KO_PITCHES`). 갤럭시 탭처럼 한 기기만 어색할 때 그 기기만 바꾼다. 안드로이드 목소리 이름(`ko-kr-x-ism-local`)은 "목소리 1 · 기기에 저장됨"으로 보인다(`koVoiceLabel`).
    - 🎤 녹음 단추는 마이크를 먼저 켜 두고 3·2·1 뒤에 녹음을 시작한다(`recStart({onCount})`, `recCountHtml`) — 빨리 말하면 앞부분이 잘렸다. 수업·ECC 연습·대화방 음성 모두.
    - 자연스러운 목소리는 기기에 저장해 다시 쓴다(키 `v2|목소리|말투|글`). 말투를 바꾸면 키 앞의 버전을 올린다.
  - 🎈 놀이: 부모님이 승인해야 열린다. 🧠 생각 놀이 4가지는 직접 만든 문제 생성기라서 매번 새 문제가 나온다.
    - 🔄 **놀이는 열 때마다 새로 시작한다**(부모님 요청). 아이별로 저장하는 놀이는 🐣 마음 친구(`profile.pet`)뿐이다. 풀숲 친구의 차·개미집·도감, 숨바꼭질 트로피·최고 단계, 새총 최고 점수(`SL_BEST`), 생각 놀이 단계(`PLAY_LV`)는 저장하지 않는다. 놀이로 받은 별(⭐)은 그대로 모인다. 예전에 저장된 `profile.itto`·`bear`·`play`·`slingBest`는 읽지 않고, 🧹 새로 시작하기가 지운다.
    - 🧱 쌓기나무 탐험대 `stk*`: 1단계부터 숨은 나무가 있다(최대 가로 4·세로 3·4층). 개수 · 위/앞/옆 모양 · 몇 개 더 쌓으면 상자 모양(`more`) · 위에서 본 모양에 층수(`topnum`). 보기에 ‘보이는 것만 센’ 흔한 실수를 넣는다(`stkHidden`).
    - 🍕 피자 가게 `pz*`: 같은 양(1/2 = 3/6) → 남은 피자 똑같이 나누기(`share`, 먹은 조각은 빈 자리) → 두 손님 주문 합치기(`sum`) → 남은 것에서 ‘전체의’ 몇 분의 몇(`part`). 틀리면 먼저 힌트, 두 번째에 정답. 처음에 맞힌 게 3번 이어지면 단계가 오른다. 분수 뒤 조사는 `pzP`(1/2은·2/4는).
    - ✏️ 한붓그리기 `ol*`: 4×4 → 5×5 → 6×6 → 7×7 → 8×8. 3단계부터 🏁 칸(`goal`)에서 끝나야 한다(깃발은 마지막에만 들어간다). 끌어서 그리므로 화면 위 층 `#olLayer`.
    - 🔍 패턴 탐정 `pt*`: 4칸·갔다 돌아오기·두 가지가 다른 주기로 바뀌기·돌면서 커지기·1,2,1,3… 개수 규칙. 3단계부터 ?가 가운데에도 숨는다. 보기는 눈에 보이는 모양(`ptLook`)으로 견준다 — 돌려도 똑같은 동그라미·네모가 두 번 나오지 않게.
    - 단계는 이번 판에만 기억한다(`PLAY_LV`, `playLevel`/`playLevelUp`, 놀이를 열 때 `playLevelNew`로 1단계부터). 3번 연속으로 맞히면 한 단계 오른다.
    - 🌱 이또 `it*`(층 `#itLayer`, 상태 `IT`): 정원 풀을 문질러 깎으면(캔버스 `destination-out`) 숨은 친구가 나타난다 → 땅속 탐험차(5자리 `IT_SEATS`, 창문을 누르면 그 친구를 고른다)에 태워 → 🚗 출발.
      - 여행 `itJourney`: 하늘로 슝~ 날아올랐다가 빙글 돌아 땅속으로 쏙(`.it-world` 400% 높이를 움직인다). 돌아올 때는 땅 위로 뿅!
      - 🐜 개미집 `itNest`: 큰 방 5개(`NEST_BASE`)가 굴로 이어지고 방마다 버블 3~4개. 빈 버블이 모자라면 아래로 더 판다(`NEST_MORE`). 설계 단위(가로 1000)로 정하고 화면 너비에 맞춰 줄인다. 버블 안쪽 여백은 %로 주지 않는다(개미집 너비 기준이 돼서 그림이 0이 된다).
      - 친구들은 `itWanderTick`(0.4초)마다 굴을 따라 다른 방에 놀러 갔다 오거나(`itVisit`) 이사한다(`itMove`, BFS 길 `itRoute`). 아이는 버블 속 친구 → 빈 버블을 눌러 걸어가게 하거나, 친구 둘을 눌러 같이 놀게 한다. 떠날 때 `itNestStop`이 걷던 친구를 가던 자리에 넣는다.
      - 친구 13종(`IT_FRIENDS` — 보통 8 · 🌈 특별 3 · 🐢 큰 친구 2, 반짝이·달님이는 드묾)은 이 앱에서 새로 그린 캐릭터다. 차·개미집 버블·도감은 이번에 연 동안만(열 때마다 빈 차·빈 개미집으로 새로 시작, 저장하지 않는다).
      - 가끔 오는 친구(`itRare`, `itChance`): 🌈 특별한 친구(`special` — 알록이·포롱이·나풀이. 풀숲에 ✨ 반짝임 힌트 `.it-glint`, 만날 때마다 ⭐)와 🐢 큰 친구(`big` — 통통이·느긋이. 쿵쿵 발소리, 정원에서 1.5배, 차에서는 창문 두 개를 이은 긴 창 — `itSize` 2, `itCarUsed`). 보통 뽑기(`itRoll`·`gdRoll`)는 `IT_POOL`만 쓴다. 확률은 특별 8%·큰 10%에서 못 만난 정원마다 +5%(최대 45%, 이번 판의 `IT.sp`·`bg`). 빈 차에 모두 탈 수 있게 자리 합이 5를 넘지 않도록 친구 수를 줄인다. 찾았는데 못 태운 특별·큰 친구는 다음 정원에 나와서 기다린다(`wait`, `itKeepWaiting`).
      - 같이 놀기 풀숲(`gdMake`)에서도 풀숲마다 30%씩 나온다. 모든 기기가 같은 풀숲을 만들어야 해서 씨앗 난수와 사칙연산·제곱근만 쓴다(`Math.hypot`은 브라우저마다 끝자리가 다를 수 있다). 큰 친구는 둘레가 가장 넓은 자리의 친구가 된다.
    - 🧸 숨바꼭질 `bh*`(층 `#bhLayer`, 상태 `BH`): 이름은 가족 설정 데이터의 `settings.hideTitle`(`bhTitle()`, 비우면 "곰돌이 숨바꼭질") — 아이 이름이 들어가서 코드에 쓰지 않는다. 설정 화면의 이름 칸은 없앴다(부모님 요청, 저장된 이름은 그대로).
      - 키즈카페 아무 데서나(`bhSpawn`, 겹치지 않게 무작위) 곰돌이가 뿅! 나왔다 숨는다. 빠르게 눌러야 해서 `pointerdown`으로 받는다. 반짝 곰돌이는 +2.
      - 🐰 토끼는 잡으면 안 된다(누르면 -1, 0 밑으로는 안 내려간다). 단계가 오를수록 자주 나온다(`bhParams().rabbit`).
      - 한 단계 60초, 목표 15·20·25…. 목표를 채우면 바로 🏆 + 다음 단계(더 빨리·더 많이). 시간 안에 못 채우면 끝. 트로피·최고 단계는 이번에 연 동안만 센다(저장하지 않는다). 화면을 숨기면 멈춘다. 세로로 긴 화면은 무대도 길게(`bhCafe(h)`).
    - 🤝 같이 놀기 `vs*`(층 `#vsLayer`, 상태 `VS`): 가족이 각자 기기에서 2~4명(`VS_MAX`) 함께 논다 — 아이들 + 부모님. 현황판 — 사람마다 🔓 놀이 허락(`activeUnlock`, 부모님은 필요 없음) · ✅ 들어왔어요 · 👍 준비됐어요. 들어온 사람이 모두 준비되면 방장(준비된 사람 가운데 id가 가장 앞서는 쪽)이 `start` {game, mode, seed, startAt, players}를 보내고 3·2·1 같이 시작.
      - 놀이(`VS_GAMES` 3가지) × 방식(`VS_MODES`): **⚔️ 개인전**(각자 겨루기) · **🤝 팀전**(모두 한 팀이 되어 목표 채우기)을 현황판에서 고른다(`ACTS.vsMode`, 처음엔 팀전). 고른 놀이·방식은 `pick` {game, mode}로 모두에게. 예전 이름(`bearTeam`)·방식 없는 신호는 `vsNorm`이 알아듣는다.
        - 🔍 틀린 그림 찾기(`sd*`, 같은 seed면 같은 그림 · 다른 곳 5개 · 90초): 개인전은 먼저 다 찾으면 1등. 팀전은 누가 찾아도 모두의 그림에 찾은 사람 색 동그라미(`sdf` {i}, `sdGot`), 90초 안에 5곳.
        - 🌱 풀숲 친구 찾기(`gd*`, 60초 `GD_SECS`): 한 풀숲의 친구 6마리(`GD_N`)를 다 찾으면 새 풀숲이 쑥 자란다(풀숲 번호 `gdR`, 씨앗 seed + r×7919 — 친구만 찾고 바로 끝나지 않게). 팀전은 같은 풀숲을 다 같이 깎는다(깎은 자국 `mow` {r, s}를 0.15초마다, `found` {r, i}, 이름표·색 테두리, 목표 한 사람당 6마리). 먼저 새 풀숲으로 간 사람의 신호는 모아 두었다가 따라간다(`gdQ`, 1.5초 뒤 `gdCatchUp`). 개인전은 똑같은 풀숲을 각자 깎고(자국은 보내지 않는다) 찾은 수만 보낸다 — 60초 뒤 더 많이 찾은 사람이 1등.
        - 🧸 숨바꼭질(`bearOpen({vs})`, `bhR()` 씨앗 난수 · 60초): 개인전은 점수 겨루기, 팀전은 다 같이 모아 목표 `vsTeamTarget(n)`(한 사람당 15마리).
      - 별: 개인전은 1등 ⭐2 · 나머지 ⭐1, 팀전은 목표를 채우면 모두 ⭐2 · 아니어도 ⭐1. 기록은 남기지 않는다(혼자 연습하기는 없앴다 — 부모님 요청).
      - 들어가는 곳: 놀이 탭 🤝 같이 놀기 · 놀이 목록의 "🤝 같이" 단추와 이또 정원·숨바꼭질 시작 카드(`ACTS.vsWith(game)`, 그 놀이를 골라 둔 채로) · 부모님은 부모님 화면 → 놀이 승인 → "🤝 아이들과 같이 놀기"(`ACTS.vsParent`, 나는 `p:부모님 id`, 나가면 연결을 끊는다).
      - 연결은 Supabase Realtime 채널 `dl-vs-<가족 해시>`(`VSN`). 아이가 자기 공간에 들어오면 조용히 붙고, 형제가 방에 들어오면 초대 카드(`#vsInvite`). 접속 표시(presence)는 채널에 들어올 때 한 번만 보낸다(`vsTrack`) — 판마다 보내면 서버가 "Client presence rate limit exceeded"로 채널을 닫았다(30초에 여섯 번). 어디 있는지·준비·고른 놀이·방식은 방송(`st`)으로. 누가 새로 보이면(다시 붙었을 때도) `st` {ask:true}로 서로 상태를 다시 묻는다. 끊기면 2.5초 뒤 다시 붙고, 시작 신호를 놓친 형제에게는 다시 보낸다.
    - 🐣 마음 친구 키우기 `pet*`(층 `#petLayer`, 상태 `PET`, 저장 `profile.pet`): 알을 톡톡 깨워 모양(토끼·곰·병아리)·색·이름을 아이가 고른다. **태어나서 🍼 아기 → 🧸 꼬마 → 🖍️ 유치원생 → 🎒 초등학생 언니까지만 자란다 — '착하고 바른 멋진 언니'로 크는 이야기**(부모님이 정한 방향).
      - 돌보기(🍎 밥 · 🫧 문질러 씻기 · 🎾 공 잡기 · 🌙 자장가 · 🤗 꾹 눌러 안아 주기)로 🌱 쑥쑥 점수(`xp`). 하루에 `PET_DAY_XP`(12)까지만 오르고(`petGrow`, 하루하루 돌보게), 할 일을 다 한 날엔 +4. 점수는 다음 단계 문턱에서 멈춘다.
      - 🌟 바른 마음 알려 주기(`PET_GOOD` 15가지): 친구가 겪은 일 → 좋은 행동·아쉬운 행동 중 고르기 → "왜 그럴까?" 까닭 셋(모두 좋은 생각). 아쉬운 쪽을 골라도 "그러면 ○○할 것 같아. 이렇게 해 볼까?"로 이끈다. 단계마다 배울 것(`PET_STAGES[i][3]`: 아기 인사·고마워 / 꼬마 미안해·나눠 쓰기·정리 / 유치원생 차례·도와주기·솔직하게·약속)을 다 배우고 점수가 차야 다음 단계(`petStage(p)`). 다 크면 멋진 언니 수업(양보·칭찬·동생 챙기기·용기·아껴 쓰기·스스로).
      - 💭 마음 알아맞히기(`PET_EMO`): 기분 고르기 → 위로하는 말 고르기. 하루 3번까지 ⭐.
      - 그림(`petSvg(p, 얼굴, 단계)`)은 자랄수록 커지고 옷차림이 바뀐다(턱받이 → 리본 → 노란 모자·꽃 이름표 → 책가방·1학년 이름표). 바뀌는 건 축하할 때(`petGrew`, `seenSt`·`P.showSt`) — 다른 걸 하는 중이면 끝난 뒤에, 층을 닫아 놓쳤으면 다음에 열 때.
      - 🎒 초등학생 언니가 되면 입학 카드(`petGradHtml`: 자란 날 `grew`·배운 바른 마음·함께 돌본 날 `days`)와 ⭐3. 그 뒤로는 더 자라지 않고 계속 같이 논다.
      - 죽거나 아프지 않는다. 오래 안 오면 "보고 싶었어!"만 하고 게이지는 10 밑으로 안 내려간다(`petDecay`).
    - 다른 회사 캐릭터(포켓몬스터·시나모롤 같은)는 그리지 않는다. 비슷한 느낌의 새 캐릭터를 이 앱 그림체로 만든다.
  - 📘 AR: `arParse`(글자에서 점수 읽기), `arBand`(술술·딱 맞는·도전), `arShelfCard`(책장 카드)
  - ✏️ 받아쓰기: 급수표 `dlists`, 아이×급수표 진도 `dprog`(문장마다 `items[i].last`가 false면 🔁 오답 노트).
    - 연습은 화면 위 층 `#dtLayer`(`DT`). 📝 공책에 쓰기(스스로 ⭕❌) · ⌨️ 패드에 쓰기(자동 채점) · 🎯 한 번만 듣고 쓰기(꼼꼼한 아이).
    - `dtGrade`: 자모로 글자를 맞춰 보고 받침·모음·첫소리·띄어쓰기(∨ 띄어 써요, ⁀ 붙여 써요)·문장 부호로 나눈다. 낱말 수가 같으면 `koVariants`로 ‘소리 나는 대로’ 쓴 것도 알아챈다.
    - 뚜뚜가 두 번 읽는다(`voice(…, {lang:'ko', style:'dict'})`). 자연스러운 목소리는 늘여 틀지 않는다(늘이면 소리가 뭉개진다).
    - 🔤 영어 철자(`sp*`, 컬렉션 `spell` 아이마다 한 줄): 글자 맞추기·듣고 쓰기·빈칸 채우기. 단어 가방 + 스피치 원고 단어(이름은 뺀다).
    - 부모님 화면 → ✏️ 받아쓰기(`dictTab`): 한 줄에 한 문장, 번호는 저절로 빠진다. 📷 사진은 `coach` `ocr`로 읽어 칸에 채운다(부모님이 확인).
  - 💭 생각: `think` 컬렉션 하나에 `type`으로 나눈다 — `q` 오늘의 질문(`TQ` 60개, 뚜뚜가 한 번 되묻고, 둘 다 답해야 서로 보인다) · `day` 하루 돌아보기 · `wonder` 궁금해 노트(내 생각 먼저) · `mission` 문제 해결 5단계 · `diary` 그림일기(`#dyLayer`, `KO_OOPS` 맞춤법 힌트).
    - 🤝 이번 주 약속은 `goals`(아이×주 한 줄). 메뉴는 `GOAL_MENU` + 부모님이 넣은 `settings.goalExtra`. 책·받아쓰기 같은 건 저절로 센다.
    - ⏱ 예상 시간: 할 일 ‘시작’ 때 몇 분 걸릴지 고르고(`estSheet`), 끝나면 비교한다(`sessions.est`/`estHit`).
    - 📖 책 한 줄 느낌: `book.feel`(느낌 낱말 칩 `BOOK_FEELS`, 🎙 말로 쓰기). 다 읽은 책만. (주인공에게 한마디 `toHero`는 없앴다 — 예전 기록은 데이터에만 남아 있다)
    - 🤖 AI 도우미(궁금해 노트 설명·일기 맞춤법)는 `settings.thinkAi === true`일 때만. 이름은 `ttMask`로 가려서 보낸다.
  - 🌏 세계: d3-geo 지구본(`G`, `worldMount`/`globeDraw`). 캔버스 하나를 계속 다시 붙여 써서 5초 동기화에도 끊기지 않는다.
    - 화면: 넓으면(760px 이상, 아이패드) 왼쪽에 지구본(`.w-left`, 머리줄 아래에 붙어 따라온다), 오른쪽에 탭 셋(`W_TABS`, `S.view.wTab`) — 🌏 나라 공부(`wStudyTab`: 고른 나라 `wSelCard` + ⭐ 오늘의 나라) · ✈️ 여행 기록(`wTripTab`: 고른 나라 여행 + 🛂 여권 + 목록) · ⭐ 가고 싶은 곳(`wWishTab`). 지구본에서 나라를 누르면 탭마다 맨 위에 그 나라 이야기가 나온다. 폰은 지구본 아래에 탭(나라를 누르면 탭까지 내려 준다). 1000px 넘으면 세계 탭만 `.wrap.wide`(1160px). 여행·가고 싶은 곳을 저장하면 그 탭으로 간다.
    - 나라 이름 `WORLD_KO`(world-atlas 110m 번호 → ISO·한국어, Node의 CLDR로 만듦), 작은 나라는 `WORLD_SMALL`(핀).
    - ⭐ 오늘의 나라 `WC` 42개국(인사말·수도·랜드마크·음식·사실 — 사실은 확인한 것만). 📖 알아봤어요 / 🔎 집 지구본에서 찾았어요(부모님 비밀번호, `pinPurpose 'globe:XX'`) → 컬렉션 `stamps`.
    - ✈️ 여행 기록 `trips`(`kidIds` 여럿, 장소 핀 `PLACES`), ⭐ 가고 싶은 곳 `wishes`, 🛂 여권은 여행·찾기 도장을 모아 보여 준다. 가 본 곳은 아이 색 하나로 칠한다.
    - 📷 사진(`photoSave`/`photoUrl`/`hydratePhotos`, `<img data-photo>`): 비공개 버킷 `memories`에 올린다. 폴더는 가족 ID의 SHA-256. 버킷이 없으면(`memories_ready()`가 없으면) 이 기기 IndexedDB `dolearn-photos`에만 둔다.
- **`api/_ai.js`**: AI 공통 코드. `provider`, `askGemini`, `askClaude`, `explain`. 이름이 `_`로 시작해서 주소로 열리지 않는다.
- **`api/dict.js`**: 사전. 모드는 `en` 영어(한국어 뜻 + 쉬운 영어 풀이), `kk` 한글.
- **`api/coach.js`**: 뚜뚜. 가족 ID가 실제로 있는지 확인한 뒤에만 동작한다. `task`는 여섯 가지다.
  - `prep`: 원고 준비
  - `speech`: 녹음 피드백 (Gemini만 — Claude는 녹음을 못 듣는다)
  - `tts`: 자연스러운 목소리. `gemini-3.8-flash-tts`를 Interactions API로 부르고, 안 되면 예전 방식으로 넘어간다. 말투 `ko`·`en`·`slow`·`speech`·`dict`(받아쓰기)·`hello`(그 나라 인사말).
    - 말투(`TTS_STYLES`)는 "평소에 말하듯 자연스럽게"를 먼저 쓰고, 하지 말 것(과장·노래하듯·로봇처럼·한 단어씩 끊기)을 함께 적는다. "다정한 선생님처럼"만 쓰면 동화 구연처럼 어색해진다.
  - `ocr`: 받아쓰기 급수표 사진 → 문장 (급수가 여러 개면 나눠서)
  - `wonder`: 궁금해 노트 — 아이 생각을 먼저 칭찬하고 쉽게 설명 + 되묻기
  - `diary`: 그림일기 맞춤법 — 고칠 낱말과 힌트만
  - 글·사진 작업(`prep`·`ocr`·`wonder`·`diary`)은 Gemini가 붐비면 `ANTHROPIC_API_KEY`가 있을 때 Claude가 이어받는다(`askText`).
- **`api/book.js`, `notify.js`, `telegram-*.js`**: 책 찾기, 텔레그램 알림.
- **데이터**: Supabase `items`(`coll` + `data` jsonb), `settings`.
  - 뚜뚜가 쓰는 컬렉션: `scripts`(원고), `speech`(아이×원고 진도 1줄).
  - 새 컬렉션: `dlists`·`dprog`·`spell`(받아쓰기·철자), `think`·`goals`(생각), `trips`·`wishes`·`stamps`(세계), `chat`·`chatrooms`(대화방).
  - 사진 저장소: 비공개 버킷 `memories` + 정책 3개(select·insert·delete, 폴더 = `encode(digest(x-family-id,'sha256'),'hex')`) + `memories_ready()`.
  - 녹음과 뚜뚜 목소리는 기기의 IndexedDB `dolearn-audio`에만 둔다. 예외: 대화방 🎤 음성 메시지는 가족끼리 들어야 해서 가족 데이터(`vclip`, 2주 뒤 지움)에 둔다.

## 개발 환경 (이 Mac)

- Node는 `~/.local/node`에 있다. 새 셸에서 안 잡히면 `export PATH="$HOME/.local/node/bin:$PATH"`.
- 로컬 실행: `vercel dev`. `.claude/launch.json`의 `vercel-dev` 설정으로 앱 내장 브라우저에서 띄운다.
- 키: `vercel dev`는 **`.env`**를 읽는다(`.env.local` 아님). 버셀의 키는 Sensitive라서 받아올 수 없으니 직접 붙여넣는다.
- 시험은 **테스트 가족**으로 한다.
  - 내장 브라우저의 `localhost:3000`은 부모님이 직접 쓰는 테스트 가족이다.
  - 자동 시험은 `127.0.0.1:3000`에서 한다. 주소가 다르면 가족 ID가 따로 생긴다.
- 서버 함수는 가짜 fetch로 시험한다(예: 사전·뚜뚜 응답 흉내). 마이크 녹음은 아이패드(https)에서만 확인할 수 있다.

## 코드 규칙

- 파일은 `index.html` 하나, 빌드 없음, 주석은 한국어로 쓴다. `innerHTML`에 넣는 글자는 반드시 `esc()`를 거친다.
- 옛 아이패드를 위해 `?.`, `??`, 정규식 lookbehind(`(?<=`)는 쓰지 않는다.
- 새 컬렉션은 `COLLS`와 `clearSample`에 추가한다.
- 스크립트는 위에서부터 한 번에 실행된다. 여러 구역에서 쓰는 것(`ACTS` 등)은 쓰는 곳보다 먼저 만든다 — 늦게 만들면 앱 전체가 멈춘다(`Cannot access … before initialization`).
- 놀이를 고치면 헤드리스 크롬으로 실제로 한 바퀴 돌려 본다(오류 0개인지). 내장 브라우저가 로그인 창 때문에 멈춰 있을 때도 쓸 수 있다.
- 한국어 조사는 받침에 맞춘다: 이름은 `subj`/`topic`/`callName`, 곳 이름은 `eunNeun`(파리는·서울은)·`eulReul`. "을(를)"처럼 쓰지 않는다.
- 5초 동기화가 `#screen`을 다시 그린다. 그래서 녹음·캔버스·입력칸은 화면 위 층(`ttLayer`)이나 모달에 둔다.
- `Store.update`는 서버에 레코드를 통째로 덮어쓴다. 진도처럼 큰 객체는 통째로 저장한다(`ttSaveProg`).
- 아이패드는 소리를 **누른 순간에** 먼저 깨워야 한다. 수업이나 퀴즈를 여는 단추에서 `nvUnlock()`을 부른다.
- AI 듣기(녹음을 Gemini로 보내기)는 부모님이 안내를 읽고 켤 때만 켜진다. 기본값은 꺼짐이다(Gemini API 약관의 18세 미만 조항 때문).
